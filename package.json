{
  "name": "CampusNet",
  "description": "dependencies of the project",
  "dependencies": {
    "express": ">=0.0.0",
    "cookie": ">=0.0.0",
    "cookie-parser": ">=0.0.0",
    "express-session": ">=0.0.0",
    "socket.io": ">=0.0.0",
    "mysql": ">=0.0.0"
  },
  "engines": {
    "node": ">=0.6"
  }
}
